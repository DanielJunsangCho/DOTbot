# /agents/background_agent.yaml
name: Branch Agent
description: Handles branch tasks for issues and creates branches
tasks:
  - name: create_branch_for_issue
    description: Create a new branch for the issue if it doesn’t exist
    action: create_branch
    branch_name: "claude/issue-${{ issue_number }}-${{ timestamp }}"
    base_branch: main
    required: true